<h1>Edit your article</h1>

<%= form_for(@article) do |f| %>
  <div id="error_explanation">
    <h2>
      <%= pluralize(@article.errors.count,"mishap") %>
      prohibited your article from being saved. I'll bet you can fix it, though.
    </h2>
    <ul>
      <% @article.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<%end%>